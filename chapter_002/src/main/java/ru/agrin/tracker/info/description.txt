 ласс Bid.
	—оздан дл€ описани€ модели за€вки.
	ѕол€:
		name, приватное поле типа String дл€ навазни€ за€вки.
		descreption, приватное поле типа String дл€ описани€ за€вки.
		dateBid, приватное поле типа calendar дл€ даты добавлени€ за€вки.
		comments[], публичный массив типа String дл€ хранени€ комментариев к за€вке.
		id, приватное поле дл€ хранени€ номера за€вки.
	ћетоды:
		getName(), возвращает им€ за€вки (поле name).
		setName(String name), добавл€ет им€ за€вки в поле name.
		getDescription(), возвращает описание за€вки.
		setDescription(String descriprion), добавл€ет описание зав€вки в поле description.
		getDateBid(), возвращает дату за€вки.
		setDateBid(Calendar date), добавл€ет дату за€вки в поле dateBid.
		getId(), возвращает номер за€вки.
		setId(int id), добавл€ет номер за€вки в поле id.
		¬се методы публичные.
		

 ласс Tracker.
	’ранит список за€вок, описывает методы работы с за€вками (добавление, редактирование,
	удаление, выведени€ списка за€вка, фильтраци€ списка, добавление комментари€).
	ѕол€:
		bids[], приватный массив типа Bid, дл€ хранени€ списка за€вок.
		countBids, приватный счетчик числа за€вок, используетс€ дл€ корректного добавлени€
			в массив за€вок новой за€вки.

	ћетоды:
		addBid(Bid bid), создает новую за€вкуи кладет ее в массив bids[].
		editBid(int id), редактирует за€вку по ее номеру.
		removeBid(int id), удал€ет за€вку по ее номеру.
		listOfBids(), возвращает список всех за€вок.
		filteredListOfBidsByDate(Calendar startDate, Calendar endDate), выводит список за€вок, 
			отфильтрованных по промежутку времени.
		filteredListofBidsByName(String name), выводит список за€вок отфильтрованных по имени.
		addComment(String comment), добавл€ет комментарий к за€вке.

 ласс Menu.
	¬ыводит пользовательское меню, обрабатывает запросы пользовател€.
	ћетоды:
		showMenu(), показывает меню выбора.
		inputOperation(String numberOfOperation), обрабатывает введенную пользователем команду.

 ласс StartTracker
	ќсновной класс, запускает программу
	ћетоды:
		main() выполн€ет метод showMenu() и inputOperation(String numberOfOperation)
		